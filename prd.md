# iUp 产品文档

- app 名称：iUp
- macOS app
- 用户故事：保持 macOS 不锁屏/不睡眠/不断网/屏幕不关闭，后文中称之为「清醒功能」；这里的手段可以参考 Amphetamine app
  - 这个功能尽可能可配置，且可以单独开启关闭
- 用户故事：对于 t0 时刻，用户最后一次有鼠标指针/键盘活动（模拟的不算）；对于 t1=t0+T 时刻，用户超过 T 时间没有鼠标指针/键盘活动（模拟的不算），则每隔 U 时间模拟鼠标随机移动（比如持续 0.5 秒的连贯移动）；对于 t2=t1+V 时刻，V 可配置，停止模拟鼠标随机移动；如果用户在 t0 时刻后有任意鼠标指针/键盘活动（模拟的不算），则返回到 t0 时刻对待；后文中称之为「活动模拟功能」；这里的手段可以参考 Amphetamine app
  - 这个功能尽可能可配置，且可以单独开启关闭
  - 这个功能在「锁定功能」锁屏时也正常工作
- 用户故事：对于 t0 时刻，用户最后一次有鼠标指针/键盘活动（模拟的不算）；对于 t1=t0+T 时刻，用户超过 T 时间没有鼠标指针/键盘活动（模拟的不算），则暂时关闭「清醒功能」；如果用户在 t0 时刻后有任意鼠标指针/键盘活动（模拟的不算），则返回到 t0 时刻对待，并且重新开启「清醒功能」；后文中称之为「临时暂停清醒功能」
  - 这个功能尽可能可配置，且可以单独开启关闭
  - 这个功能在「锁定功能」锁屏时也正常工作
- 用户故事：在点击状态栏图标后在下拉框中点击按钮/按下特定全局快捷键（例如 control + option + command + L）时，在所有屏幕顶层展示一个全黑的遮罩，且捕获用户的鼠标指针/键盘活动（模拟锁屏）；所有屏幕的遮罩上能显示自定义文字，为了防烧屏，每隔一段时间后文字的位置会发生变化，使得显示器的每个位置都有同等概率被覆盖，文字是亮灰色细体；所有屏幕的遮罩上都有解锁按钮，使用鼠标指针按下后可以通过 Touch ID 或者输入 macOS 用户密码解锁，按下特定快捷键（例如 control + option + command + L）也可以解锁，解锁后退出锁屏模式，这个按钮同样也要有防烧屏处理；后文中称之为「锁定功能」；这里的手段可以参考 Lockpaw app
  - 这个功能尽可能可配置，且可以单独开启关闭
  - 这个功能模拟锁屏效果，但不是使用 macOS 的系统锁定功能
- 用户故事：如果没有内置显示器，则请不要允许用户启用这个功能。进入「锁定功能」的锁屏模式时，如果有内置显示器，则关闭外置显示器（可以是断开信号之类的，技术上做不到可以不实现）；如果有内置显示器，则将内置显示器的亮度调到 0（MacBook 显示器亮度为 0 时类似于关闭显示器）；如果用户有任意鼠标指针/键盘活动（模拟的不算），则只恢复内置显示器的亮度；在上次鼠标指针/键盘活动（模拟的不算）后，如果用户超过 T 时间没有鼠标指针/键盘活动（模拟的不算），则将内置显示器的亮度调到 0；如果用户退出了「锁定功能」的锁屏模式，则恢复内置显示器亮度并恢复打开外置显示器（可以是重新连接信号之类的，技术上做不到可以不实现）
  - 这个功能尽可能可配置，且可以单独开启关闭
- 用户故事：点击状态栏，则显示下拉框，下拉框中有「启动清醒会话/停止清醒会话」按钮，按下后切换清醒会话状态；在每个清醒会话中，激活「清醒功能」「活动模拟功能」「临时暂停清醒功能」；不在清醒会话时则不激活这些功能
- 用户故事：点击状态栏，则显示下拉框，下拉框中有锁定按钮，按下后进入「锁定功能」锁屏模式
- 用户故事：点击状态栏，则显示下拉框，下拉框中有设置按钮，按下后显示设置 UI
- 用户故事：点击状态栏，则显示下拉框，下拉框中有退出按钮，按下后退出 iUp
- 用户故事：iUp 可以配置：开机自动启动 app、运行时不在 dock 中显示图标、app 启动时自动启动清醒会话等等
- 用户故事：iUp 大概率需要无障碍权限，如果没获取到权限，请提示用户在 macOS 系统设置中给予相应的权限
- iUp 的核心机制在 Amphetamine app 和 Lockpaw app 中都有实现，包括但不限于保持 macOS 不锁屏/不睡眠/不断网/屏幕不关闭、连贯地模拟移动鼠标指针、监测鼠标指针/键盘活动（模拟的不算）、在所有屏幕上展示类似锁屏效果的遮罩（包括捕获所有键盘活动）、防烧屏机制、Touch ID/密码解锁机制、全局快捷键监听、计时机制、状态栏图标、开机自动启动 app、隐藏 dock 图标、获取无障碍权限
